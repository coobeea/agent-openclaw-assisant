# 🌐 OpenClaw 系统访问地址

> 更新时间：2026-04-10 20:24

---

## ✅ 前端界面（已启动）

### Vue3 Web 界面
```
🌐 http://localhost:3001
```

**功能**：
- ✅ 用户注册/登录
- ✅ 创建智能体
- ✅ 智能体列表
- ✅ 实时对话
- ✅ 对话历史查看

**启动方式**：
```bash
./start-frontend.sh
```

**停止方式**：
```bash
# 如果在前台运行，按 Ctrl+C
# 如果在后台运行：
ps aux | grep vite | grep -v grep | awk '{print $2}' | xargs kill
```

---

## ✅ 后端 API（已启动）

### Go 调度服务
```
🌐 http://localhost:30080
```

**主要端点**：
| 端点 | 地址 |
|------|------|
| 健康检查 | http://localhost:30080/health |
| 系统状态 | http://localhost:30080/api/status |
| 用户注册 | http://localhost:30080/api/users/register |
| 用户登录 | http://localhost:30080/api/users/login |
| 创建智能体 | http://localhost:30080/api/agents/create |
| 智能体列表 | http://localhost:30080/api/agents/list |
| 发送对话 | http://localhost:30080/api/chat |
| 对话历史 | http://localhost:30080/api/chat/history |
| 容器状态 | http://localhost:30080/api/containers/status |

---

## 🧪 快速测试

### 1. 访问前端
```
在浏览器打开：http://localhost:3001
```

### 2. 测试 API
```bash
# 健康检查
curl http://localhost:30080/health

# 系统状态
curl http://localhost:30080/api/status

# 容器池状态
curl http://localhost:30080/api/containers/status
```

### 3. 完整流程测试
```bash
./test-api.sh
```

---

## 📊 服务状态

### 查看所有服务状态
```bash
./status.sh
```

### 检查前端进程
```bash
ps aux | grep vite | grep -v grep
```

### 检查后端服务
```bash
kubectl get pods -n openclaw
```

---

## 🎯 完整使用流程

### Step 1: 打开浏览器
```
访问：http://localhost:3001
```

### Step 2: 注册用户
在前端界面点击"注册"，输入：
- 用户名：demo
- 密码：123456

### Step 3: 创建智能体
登录后，点击"创建智能体"按钮：
- 名称：客服助手
- 描述：专业的客服智能体

### Step 4: 开始对话
选择创建的智能体，在输入框发送消息：
- "你好"
- "请介绍一下你自己"
- "你能帮我做什么？"

---

## 📂 数据存储位置

### 智能体工作空间
```
/Users/lifeng/git/git-claw/agent-openclaw-assisant/workspace/lobsters/agents/
```

### 数据库
```
/Users/lifeng/git/git-claw/agent-openclaw-assisant/workspace/lobsters/database/
```

### 对话历史
存储在 PostgreSQL 数据库的 `conversations` 表中

---

## 🔧 管理脚本

| 脚本 | 用途 |
|------|------|
| `./start-frontend.sh` | 启动前端（前台运行） |
| `./start-services.sh` | 启动后端服务 |
| `./stop-services.sh` | 停止后端服务 |
| `./restart-services.sh` | 重启后端服务 |
| `./status.sh` | 查看系统状态 |
| `./test-api.sh` | API 完整测试 |

---

## 🐛 故障排查

### 前端无法访问

**问题**：http://localhost:3001 无法打开

**解决**：
```bash
# 1. 检查前端进程
ps aux | grep vite

# 2. 如果没有运行，启动前端
./start-frontend.sh

# 3. 查看前端日志
cat /tmp/frontend-vue.log
```

### API 无法访问

**问题**：http://localhost:30080 无法访问

**解决**：
```bash
# 1. 检查后端服务
./status.sh

# 2. 如果服务未运行，启动服务
./start-services.sh

# 3. 查看日志
kubectl logs -n openclaw -l app=openclaw-scheduler --tail=50
```

### 前端显示错误

**问题**：前端界面显示连接错误

**解决**：
```bash
# 1. 确认后端 API 正常
curl http://localhost:30080/health

# 2. 检查浏览器控制台（F12）
# 3. 确认 CORS 配置正确
```

---

## 📱 移动端访问（可选）

如果需要在同一局域网的其他设备访问：

### 1. 启动前端时暴露网络
```bash
cd frontend-vue
npm run dev -- --host
```

### 2. 获取本机 IP
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### 3. 在其他设备访问
```
http://<本机IP>:3001
```

---

## 🎉 完整系统架构

```
┌─────────────────────────────────────────┐
│   浏览器 http://localhost:3001           │
│   Vue3 前端界面                          │
└───────────────┬─────────────────────────┘
                │ HTTP API
┌───────────────▼─────────────────────────┐
│   http://localhost:30080                │
│   Go 调度服务（Kubernetes）              │
└───────────────┬─────────────────────────┘
                │ K8s API
┌───────────────▼─────────────────────────┐
│   OpenClaw 容器池（5 个 Pod）            │
│   + PostgreSQL + Redis + MinIO          │
└─────────────────────────────────────────┘
```

---

**当前状态**：✅ 前端和后端都已运行

**快速访问**：
- 🌐 前端：http://localhost:3001
- 📡 API：http://localhost:30080

**下一步**：在浏览器打开 http://localhost:3001 开始使用！
