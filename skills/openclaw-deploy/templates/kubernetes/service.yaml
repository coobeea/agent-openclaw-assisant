apiVersion: v1
kind: Service
metadata:
  name: openclaw-INSTANCE_ID-svc
  namespace: openclaw
  labels:
    app: openclaw
    instance: INSTANCE_ID
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: openclaw
    instance: INSTANCE_ID
